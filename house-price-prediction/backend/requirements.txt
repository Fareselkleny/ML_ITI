# Pinned to the exact versions used to train and export
# backend/models/house_price.pkl (see notebooks/house_price_model.ipynb).
# scikit-learn version MUST match the training environment or the pickled
# Pipeline may fail to load / behave differently.

fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
scikit-learn==1.8.0
pandas==3.0.2
numpy==2.4.4
joblib==1.5.3

# Testing
pytest==9.1.1
httpx==0.28.1
